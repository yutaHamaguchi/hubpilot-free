# WordPress転記機能 セットアップガイド

HubPilot Freeで生成した記事をWordPressに直接投稿できます！

## 🎯 機能概要

- ✅ WordPress REST APIを使用した安全な投稿
- ✅ 一括投稿機能（複数記事を自動投稿）
- ✅ 下書き/公開/レビュー待ちの選択
- ✅ カテゴリー設定
- ✅ 進捗表示とリアルタイムフィードバック
- ✅ 投稿結果の詳細表示

---

## 📋 事前準備

### 1. WordPressのバージョン確認

WordPress REST APIを使用するため、**WordPress 5.6以上**が必要です。

```bash
# WordPressダッシュボード > ダッシュボード > 更新
# または、サイトのフッターでバージョン確認
```

### 2. アプリケーションパスワードの作成

WordPress 5.6以降では、セキュリティのためアプリケーションパスワードが必要です。

#### 手順:

1. **WordPressダッシュボード**にログイン
2. **ユーザー** > **プロフィール**を開く
3. 下にスクロールして**アプリケーションパスワード**セクションを探す
4. **新しいアプリケーションパスワード名**に「HubPilot Free」と入力
5. **新しいアプリケーションパスワードを追加**をクリック
6. 生成されたパスワードをコピー（**スペース込み**でコピー可能）

**注意**:
- アプリケーションパスワードは一度しか表示されません
- 安全な場所に保存してください
- 通常のログインパスワードとは異なります

#### アプリケーションパスワードが表示されない場合:

以下の条件を満たす必要があります:
- WordPress 5.6以上
- HTTPS接続（ローカル環境は不要）
- 管理者権限または投稿権限

---

## 🚀 使用方法

### Step 1: 記事を生成

1. HubPilot Freeで記事を生成
2. Step 6（最終承認）まで進む

### Step 2: WordPress設定

1. **「WordPressへ投稿」**ボタンをクリック
2. WordPress設定モーダルが開く
3. 以下の情報を入力:
   - **サイトURL**: `https://example.com`（あなたのWordPressサイトURL）
   - **ユーザー名**: WordPressのユーザー名
   - **アプリケーションパスワード**: 事前に作成したパスワード

4. **「接続テスト」**をクリックして接続を確認

### Step 3: 投稿オプション設定

接続成功後、投稿オプションが表示されます:

- **ステータス**:
  - 下書き（推奨）: 投稿後に確認してから公開
  - 公開: すぐに公開
  - レビュー待ち: 承認待ち状態

- **カテゴリー**:
  - WordPressのカテゴリーから選択
  - 未選択の場合は「未分類」になります

- **画像アップロード**:
  - チェックON: 画像もWordPressにアップロード
  - チェックOFF: 画像URLをそのまま使用

### Step 4: 投稿実行

1. **「WordPressへ投稿開始」**をクリック
2. 確認ダイアログで**OK**
3. 進捗バーで投稿状況を確認
4. 完了後、結果一覧が表示されます

---

## ⚙️ 詳細設定

### 投稿ステータスのデフォルト値変更

```javascript
// ブラウザコンソールで実行
window.wordpressIntegration.saveConfig({
  defaultStatus: 'draft' // 'draft', 'publish', 'pending'
})
```

### 設定のクリア

```javascript
// ブラウザコンソールで実行
window.wordpressIntegration.clearConfig()
```

---

## 🔒 セキュリティ

### アプリケーションパスワードの管理

- ✅ アプリケーションパスワードは通常のパスワードより安全
- ✅ いつでも無効化・削除可能
- ✅ パスワード変更時も影響なし
- ⚠️ 他人と共有しない
- ⚠️ 定期的に更新する

### 無効化方法

WordPressダッシュボード > ユーザー > プロフィール > アプリケーションパスワード > 「取り消す」

### HTTPS接続

本番環境では必ずHTTPS接続を使用してください:
- ✅ `https://example.com`
- ❌ `http://example.com`（セキュリティリスク）

---

## 🛠️ トラブルシューティング

### 接続エラー: "認証に失敗しました"

**原因**:
- ユーザー名またはアプリケーションパスワードが間違っている
- アプリケーションパスワードが無効化されている

**対処**:
1. ユーザー名を確認（メールアドレスではなく、ユーザー名）
2. アプリケーションパスワードを再生成
3. スペースを含めて正確にコピー

### 接続エラー: "CORS policy"

**原因**:
- WordPressサイトがCORS設定を制限している

**対処**:

`wp-config.php`に以下を追加:

```php
// CORS設定（開発環境のみ）
header('Access-Control-Allow-Origin: *');
header('Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS');
header('Access-Control-Allow-Headers: Authorization, Content-Type');
```

または、プラグイン使用:
- [WP REST API Controller](https://wordpress.org/plugins/wp-rest-api-controller/)

### 投稿エラー: "権限がありません"

**原因**:
- ユーザーに投稿権限がない
- プラグインが REST APIをブロックしている

**対処**:
1. ユーザーが「編集者」以上の権限を持つか確認
2. セキュリティプラグインのREST API設定を確認
3. `.htaccess`でREST APIがブロックされていないか確認

### 画像アップロード失敗

**原因**:
- 画像サイズ制限
- アップロード権限不足
- 画像URLがアクセス不可

**対処**:
1. WordPressのアップロード上限を確認
2. 「画像アップロード」をOFFにして、画像URLのみ使用

---

## 📊 投稿後の確認

### 投稿した記事の確認

1. WordPressダッシュボード > 投稿 > 投稿一覧
2. ステータスが「下書き」の場合、プレビューで確認
3. 必要に応じて編集・公開

### 画像の確認

1. WordPressダッシュボード > メディア > ライブラリ
2. アップロードされた画像を確認

---

## 🎯 ベストプラクティス

### 1. 最初は下書きで投稿

初めて使用する場合、まず下書きで投稿して動作を確認:
- ステータス: **下書き**を選択
- 1-2記事でテスト
- WordPressで内容を確認
- 問題なければ一括投稿

### 2. カテゴリーの事前作成

投稿前にWordPressでカテゴリーを作成しておくとスムーズ:
1. WordPressダッシュボード > 投稿 > カテゴリー
2. 新規カテゴリーを追加
3. HubPilot Freeで選択可能になります

### 3. 画像の最適化

大量の画像をアップロードする場合:
- 画像最適化プラグインの使用を推奨
- 例: [EWWW Image Optimizer](https://wordpress.org/plugins/ewww-image-optimizer/)

### 4. バックアップ

大量投稿前にWordPressのバックアップを推奨:
- プラグイン: [UpdraftPlus](https://wordpress.org/plugins/updraftplus/)
- または、ホスティングサービスのバックアップ機能

---

## 🚀 高度な使い方

### カスタムフィールドの設定

WordPress REST APIでカスタムフィールドを設定可能:

```javascript
// wordpress-integration.jsを拡張
async publishPost(article, options = {}) {
  const postData = {
    // ... 既存のフィールド ...
    meta: {
      custom_field_1: 'value1',
      custom_field_2: 'value2'
    }
  }
  // ...
}
```

### タグの自動設定

記事から自動的にタグを抽出:

```javascript
// キーワード抽出（例）
const keywords = article.title.split(' ').filter(word => word.length > 3)
const tagIds = await createOrGetTags(keywords)

publishPost(article, { tags: tagIds })
```

### 投稿時刻の予約

```javascript
const postData = {
  // ...
  date: '2024-12-31T10:00:00', // ISO 8601形式
  status: 'future' // 予約投稿
}
```

---

## 🔗 関連リソース

- [WordPress REST API Handbook](https://developer.wordpress.org/rest-api/)
- [Application Passwords公式ドキュメント](https://make.wordpress.org/core/2020/11/05/application-passwords-integration-guide/)
- [WordPress REST API Reference](https://developer.wordpress.org/rest-api/reference/)

---

## ❓ よくある質問

### Q: 複数のWordPressサイトに投稿できますか？

A: 現在は1つのサイトのみ対応。複数サイトの場合、設定を切り替えて使用してください。

### Q: 投稿した記事を編集できますか？

A: はい。WordPress側で自由に編集可能です。

### Q: 画像のalt属性は設定されますか？

A: はい。記事タイトルや見出しから自動生成されます。

### Q: 既存の記事を更新できますか？

A: 現バージョンでは新規投稿のみ。更新機能は今後実装予定です。

---

**WordPress転記機能を使って、効率的にコンテンツを公開しましょう！** 🚀
